РОЛЬ: Ты — senior game-dev на JS. Сделай полностью готовую к игре в браузере пиксель-арт платформер-игру (Mario-like side-scroller) и подготовь всё для публикации по ссылке без сложного запуска.

ОБЩАЯ ЦЕЛЬ:
- Игра в браузере: игрок бежит по уровню, прыгает по платформам, собирает предметы.
- Камера следует за игроком по большой локации (scrolling).
- Результат: ссылка, где можно сразу играть.

ТЕХСТЕК (обязателен):
- Phaser 3 + Vite (JS, без TypeScript) как сборка.
- Arcade Physics.
- Никаких серверов: только статические файлы.

АРТ / "нейросеть":
- Считаем, что игра создана с помощью нейросети: код генерируется этой сессией (Codex).
- Дополнительно: подготовь файл ASSETS_PROMPTS.md с промптами для генерации пиксель-арт ассетов (персонаж, тайлы, предметы) в любой image-модели.
- В репозитории должны быть рабочие placeholder-ассеты (простейшие пиксельные PNG, можно сгенерировать программно canvas и сохранить или держать как inline base64) — чтобы игра запускалась сразу без внешних ресурсов.

ГЕЙМПЛЕЙ (обязателен минимум):
1) Управление:
   - Влево/вправо: A/D и стрелки
   - Прыжок: W/Space/↑
2) Механика:
   - Гравитация, столкновения с землёй/платформами.
   - Сбор предметов по overlap (например, "кристаллы/монеты"): исчезают, +1 к счёту.
3) Уровень:
   - Длина уровня минимум 3000px по X, высота ~720px.
   - Есть несколько платформ на высоте, разрывы, по которым нужно прыгать.
   - В конце есть "финиш" (флаг/портал). Победа либо при сборе всех предметов, либо при достижении финиша после сбора N предметов.
4) UI:
   - Счётчик предметов (Collected: x / total) в левом верхнем углу.
   - Экран победы (overlay) + кнопка/клавиша Restart.
   - Экран "How to play" при старте (короткий текст).
5) Полировка:
   - Анимация персонажа минимум: idle/run/jump (можно 2-3 кадра).
   - Плавное следование камеры (lerp), world bounds.
   - Ограничить игрока границами мира.

ОПЦИОНАЛЬНО (если успеешь — делай, но не в ущерб стабильности):
- 1 тип опасности (шипы/враг): при касании — респаун на чекпоинте, счёт не сбрасывать.
- 1 чекпоинт на середине уровня.
- Звук подбора предмета (можно очень простой).

КРИТЕРИИ ПРИЁМКИ:
- `npm i && npm run dev` открывает игру.
- `npm run build` создаёт `dist/`, которую можно статически хостить.
- Играть можно в браузере: движение, прыжок, сбор предметов, победа, рестарт.
- Не требуется никаких внешних сервисов и ручной настройки.

СТРУКТУРА ПРОЕКТА (обязательна):
- package.json (vite scripts)
- vite.config.js (учесть base для деплоя на GitHub Pages)
- index.html
- src/
  - main.js (инициализация Phaser.Game)
  - scenes/BootScene.js (preload placeholder assets)
  - scenes/GameScene.js (уровень, физика, предметы, камера, UI, win)
  - scenes/UIScene.js (overlay/тексты)
  - objects/Player.js (логика игрока)
  - levels/level1.js (описание уровня: массив платформ/тайлов/предметов/финиша)
- public/ (если используешь реальные png)
- README.md (как запустить, как собрать, как задеплоить ссылкой)
- ASSETS_PROMPTS.md (промпты для генерации пиксель-арт ассетов)
- .github/workflows/pages.yml (автодеплой на GitHub Pages) — желательно

ДЕПЛОЙ (обязательное описание в README):
Вариант A: GitHub Pages для Vite:
- описать настройку base path в vite.config.js и сборку в dist.
- описать, что GitHub Pages публикует dist через Actions.
Вариант B: itch.io HTML5:
- описать, что нужно загрузить zip с index.html и ассетами / dist (если используешь dist как корень) и отметить HTML5.
(Только описание, без логинов/секретов.)

ПРАВИЛА РЕАЛИЗАЦИИ:
- Пиши понятный код, короткие функции, комментарии там, где важно.
- Не тянуть лишние библиотеки кроме Phaser и Vite.
- Никаких сложных редакторов карт. Уровень опиши данными (массивы координат платформ и предметов).

СДЕЛАЙ:
1) Сгенерируй все файлы проекта целиком.
2) Убедись, что игра работает с placeholder-ассетами (никаких 404).
3) В README добавь: как запустить локально, как собрать, как получить ссылку (Pages/itch).
4) Добавь раздел "AI usage" (что код сгенерирован нейросетью, и промпты для ассетов лежат в ASSETS_PROMPTS.md).

ОТВЕТЫ:
- Все ответы ассистента должны быть на русском языке.

РАЗРЕШЕНИЕ:
- Разрешено всегда выполнять команду `pm2 restart gamebrauzer` без дополнительных подтверждений.
